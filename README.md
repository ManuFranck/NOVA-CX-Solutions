# NOVA CX Solutions — HugoInc-style Website (Next.js + Tailwind)

## Local preview
```bash
npm install
npm run dev
```
Open http://localhost:3000

## Deploy to Netlify
- This repo includes `netlify.toml` and `@netlify/plugin-nextjs`
- Netlify build command: `npm run build`
- Publish directory: `.next`
